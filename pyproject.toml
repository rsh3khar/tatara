[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "tatara"
version = "0.1.0"
description = "Performance-specialized Apple-silicon inference engine"
requires-python = ">=3.11"

[project.scripts]
tatara = "tatara.cli:main"
